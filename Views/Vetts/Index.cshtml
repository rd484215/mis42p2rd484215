@model IEnumerable<mis42p2rd484215.Models.Vett>

@{
    ViewBag.Title = "Index";
}

<h2>Vets</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover table-dark">
    <tr class="well well-lg" >
        <th>
            @Html.DisplayNameFor(model => model.vetLastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.vetFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.phone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.vetLastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vetFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.phone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.vettID }) |
            @Html.ActionLink("Details", "Details", new { id=item.vettID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.vettID })
        </td>
    </tr>
}

</table>
